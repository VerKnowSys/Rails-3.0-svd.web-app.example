#!/bin/sh

# svd init
#ENVIRONMENT
# svd end

echo "Entering ${RUBY_ROOT}" >> ${RUBY_LOG_FILE}
cd ${RUBY_ROOT}
bundle exec rails s -p ${RUBY_PORT} -e ${RUBY_ENV} -b 127.0.0.1 -P ${RUBY_PID_FILE} >> ${RUBY_LOG_FILE}
